<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.salary.mapper.PositionMapper">

    <resultMap id="PositionResultMap" type="com.salary.entity.Position">
        <id property="id" column="id"/>
        <result property="deptId" column="dept_id"/>
        <result property="positionName" column="position_name"/>
        <result property="salaryMin" column="salary_min"/>
        <result property="salaryMax" column="salary_max"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="isManagerPosition" column="is_manager_position"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 查询指定部门的所有启用岗位 -->
    <select id="selectByDeptId" resultMap="PositionResultMap">
        SELECT `id`, `dept_id`, `position_name`, `salary_min`, `salary_max`, `description`, `status`, `is_manager_position`, `create_time`, `update_time`
        FROM `t_position`
        WHERE `dept_id` = #{deptId}
          AND `status` = 1
        ORDER BY `position_name`
    </select>

</mapper>
