# DEV_PLAN
168. [x] 请假申请列表与详情的日期/空值展示修正
   - 现状：用户反馈新提交的请假申请在员工端、经理端和管理员端会错误显示为 `0天`，且管理员/经理列表看不到“涉及日期”，申请详情中的申请时间仍带有 `T` 原始时间串，影响演示观感和理解。
   - 处理方向：已将前端展示改为“请假天数为空时显示 `-` 而不是 `0天`”，并在管理员/经理端申请列表新增“涉及日期”列；员工端与管理员端申请详情中的申请时间统一做格式化，去掉 `T`，同时保留涉及日期单独展示。
167. [x] 请假申请按事假/病假拆分并自动回写考勤
   - 现状：用户要求保留“考勤数据”列表现有结构，只增强请假展示和审批联动；员工端需能选择事假/病假并填写请假天数，经理审批通过后自动累计到当月考勤，考勤详情中明确拆出事假天数和病假天数，薪资详情继续只展示汇总后的考勤扣款金额。
   - 处理方向：已为 `t_attendance_apply` 补充 `leave_type`、`leave_days` 字段，员工端申请表新增事假/病假和请假天数输入；审批通过时自动按“事假 1 天记 1、病假 1 天记 0.5”回写 `t_attendance_record.leave_days`，并累计 `sick_leave_days` 供考勤详情展示；同时将请假扣款改为按折算请假天数乘日薪计入 `attend_deduct`，管理端考勤列表补 hover 明细，考勤/申请详情同步显示拆分后的请假信息。
166. [x] 副本(2)AIGC修订范围按句级别收窄
   - 现状：用户指出上一轮根据 AIGC 检测报告的处理存在“按章节整段改、没有严格对照命中内容”的问题，要求重新按 PDF 中实际命中的句子收窄修改范围。
   - 处理方向：已对 `1.2`、`2.2.1`、`4.1` 中几处改动过宽的段落重新拆分，仅保留命中句的删除线和红字替换，其余未命中的原文恢复为正常文本。
165. [x] 副本(2)按AIGC检测报告红黄片段做一轮审阅式降重
   - 现状：用户提供 `AIGC检测报告.pdf`，要求按照检测结果对当前论文中红色和黄色命中段进行修改，并继续保留“旧句删除线 + 新句红字”的审阅痕迹。
   - 处理方向：已比对检测报告与当前 `基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx`，对摘要、研究背景与意义、文献综述、技术介绍、可行性分析、功能需求、性能需求、实现章节总述、测试目的和结论中的高/中风险段落进行定点改写。
164. [x] 副本(2)正文补入参考文献[15]
   - 现状：用户指出当前可查重版本中正文引用缺少 `[15]`，要求只找一个合适位置把这条文献自然塞回正文。
   - 处理方向：已在 `2.2.1 功能需求分析` 的“部门经理端”段落中，将“查看部门薪资总额、人均薪资等统计报表”与 `[15]` 关联，当前正文引用已覆盖 `[1]` 至 `[21]`。
163. [x] 副本(2)本轮复查后的正文口吻、图题和参考文献再次审阅式修订
   - 现状：用户要求基于最新替换后的 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx` 再检查一遍，并继续按“旧字删除线 + 新字红色”的方式回正文，不直接改图片本体。
   - 处理方向：已对第 `2/3/4/5` 章若干正文段、`图3-6` 图题、测试结论与结论段进行再次审阅式修订；同时补齐正文对 `21` 条参考文献的使用痕迹，并回正多条明显错误或不完整的参考文献条目。
162. [x] 图3-8按数据库映射补全属性并重建属性连线
   - 现状：用户要求在当前 `fig3-8-er-candidate-f.drawio` 基础上把属性补回去，并指出现有属性线只是两点绘制，未真正连接到属性椭圆和实体边框。
   - 处理方向：已依据 `图3-8实体与数据库表映射.md` 的“论文图建议显示属性”重建属性层，为 12 个实体补入数据库对应属性，并删除旧属性椭圆与旧属性线；新属性线改为 draw.io 原生 `source/target` 绑定方式，使属性与实体保持真实连接关系。
161. [x] 图3-8实体与数据库表映射沉淀为复用文档
   - 现状：用户指出之前已经多次读取过数据库表结构，不希望后续每次修改图3-8都重新查库、重新总结，希望把本次已确认的实体对应关系和字段范围固定记录下来，便于后续直接复用。
   - 处理方向：已在 `E:\bishe\codexlunwen\assets\mermaid\图3-8实体与数据库表映射.md` 中沉淀 12 个图中实体与真实数据库表的对应关系、每张表的真实字段清单，以及论文图建议优先展示的属性集；后续改图3-8时默认优先读取该文件。
160. [x] 基于 fig3-8-er-candidate-f 重建干净版可编辑 drawio
   - 现状：用户确认此前导错了文件，要求不要继续基于旧 `fig3-8-er-drawio-v1.drawio` 修补，而是直接以 `fig3-8-er-candidate-f` 为底稿重画；同时指出旧 drawio 线条混乱，且部分属性文字错误显示为 `<u>...</u>` 原始标签。
   - 处理方向：已读取 `fig3-8-er-candidate-f.svg`，按其中的矩形、菱形、椭圆、关系线和基数文本重新生成一份全新的 `fig3-8-er-candidate-f.drawio`，所有元素均为根层独立节点，支持单独编辑；文本直接写入中文内容，不再夹带 `<u>` 原始标签。
159. [x] 图3-8恢复普通可编辑结构并清除边线拐点残留
   - 现状：用户反馈当前 `.drawio` 在拖动时仍像整张图片一样整体联动，且圈出的若干区域仍存在不希望出现的折弯感，说明上轮把 `1/N` 挂在关系菱形下方虽然带来了联动，但影响了自由编辑体验。
   - 处理方向：已将图3-8中全部 `1/N` 基数文本从关系菱形子节点恢复为根层独立文本，并把所有边线统一改为 `noEdgeStyle=1 + edgeStyle=none + jettySize=0` 的最简直线样式，同时清空边几何中的所有残留子控制点，优先保证 draw.io 中可单独选中和自由编辑。
158. [x] 图3-8关系菱形与1/N标签联动拖拽
   - 现状：用户进一步指出图3-8除了局部排版仍需收紧外，还希望在 draw.io 中拖动关系菱形时，关联的 `1/N` 基数标注也能一起移动，避免每次手工分别调整。
   - 处理方向：已将 `fig3-8-er-drawio-v1.drawio` 中 18 组关系对应的 `1/N` 文本节点改为挂载在各自菱形节点下的子元素，并同步微调标题、用户账号区、员工中心区和薪资记录区部分属性位置，使后续在 draw.io 中拖动关系时基数标注会跟随联动。
157. [x] 图3-8 drawio 连线改为点对点直线并避开属性重叠
   - 现状：用户在 draw.io 打开 `fig3-8-er-drawio-v1.drawio` 后指出，当前关系线仍不是需要的点对点直线，且部分连线与属性椭圆发生叠加，影响论文图的整洁度。
   - 处理方向：已将该图全部边线样式统一改为直线连接，并对公告、部门、岗位、员工、绩效、薪资记录、薪资反馈、社保配置、薪资发放、个税累计等周边属性节点的坐标进行外移，优先消除员工区和薪资记录区的压线问题，继续以同一个 `.drawio` 源文件作为后续迭代底稿。
156. [x] 图3-8数据库E-R图可编辑 drawio 源文件生成
   - 现状：用户明确要求不要继续提供 Next AI Draw.io 提示词，而是需要一个可直接打开、检查并持续迭代修改的 `.drawio` 源文件，用于后续逐轮修正布局、连线和实体属性。
   - 处理方向：已通过 UTF-8 Python 生成脚本创建 `E:\bishe\codexlunwen\assets\mermaid\fig3-8-er-drawio-v1.drawio`，并完成 XML 解析校验与中文关键词校验，确认文件中已正确写入“图3-8 数据库E-R图”“用户账号”“员工”“薪资记录”等核心标签，可直接在 draw.io 中打开继续修图。
155. [x] 副本(2)数据库概念设计文字口径改为按目标ER图描述
   - 现状：用户指出 `3.3.1 数据库概念设计` 的文字不应继续迁就当前尚未替换的旧 `图3-8`，而应提前按照准备换上的新 E-R 图结构来表述，避免“正文和即将替换的图片不一致”。
   - 处理方向：已将 `基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx` 中 `3.3.1` 的红字修订文本改为围绕“用户账号、部门、岗位、员工、考勤记录、绩效记录、薪资结构、薪资记录、薪资反馈、社保配置、薪资发放、个税累计”这 12 个核心实体展开，并同步回正 `图3-8` 下方的红字换图说明。
154. [x] 副本(2)第3至5章正文按审阅痕迹继续回正并补图3-8待换图提示
   - 现状：用户要求下一轮不直接替换图片，而是先将论文中仍需修改的正文继续按“旧句删除线 + 新句红字”的方式处理；若某张图后续需要更换，则只在图下注释处补红字提示，方便快速定位。
   - 处理方向：已对 `基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx` 中第 `1.3`、第 `3`、第 `4`、第 `5` 章若干残留问题做审阅式修订，并在 `图3-8` 图题下新增红字待换图说明，暂不替换图片本身。
153. [x] 图3-8改为手工定坐标无交叉布局并补充无交叉提示词
   - 现状：用户明确指出上一版 `图3-8` 仍然过于凌乱，要求整体排版更整齐，并尽量消除交叉线条。
   - 处理方向：已新增手工定坐标版本 `fig3-8-er-candidate-d.svg`，按左右分区和双核心实体方式重排实体、联系与属性；同时在 `图3-8-ER图-next-ai-draw-io提示词.md` 中新增“无交叉精修版”提示词，专门约束固定网格、水平垂直折线和不得交叉的主关系线。
152. [x] 图3-8概念E-R图再收紧并补充 Next AI Draw.io 专用提示词
   - 现状：用户要求继续调整 `图3-8` 的论文式 E-R 图，并明确要一份可以直接发给 `next-ai-draw-io` 的提示词，用于后续进一步重绘。
   - 处理方向：已新增更紧凑的概念 E-R 图源文件 `fig3-8-er-candidate-c.puml`，并落地图像 `fig3-8-er-candidate-c.png`；同时新增 `图3-8-ER图-next-ai-draw-io提示词.md`，写明黑白论文风格、Chen 概念模型、核心实体布局和基数关系要求，便于直接交给外部绘图工具继续细化。
151. [x] 副本(2)第3章正文先行回正并启动图3-8候选ER图
   - 现状：用户要求先开始绘制 `图3-8` 的候选 E-R 图，同时先修改论文第 3 章对应正文但暂不替换图片，以便先审正文表述是否合适。
   - 处理方向：已在 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx` 中先行回正 `3.1`、`3.2.5`、`3.3.1` 三处段落，并保持“旧句删除线 + 新句红字”；同时基于真实数据库 18 张核心表编写了 `fig3-8-er-candidate-a.puml` 候选源码，并完成一版黑白 E-R 图候选布局预览。
150. [x] 黑白H图提示词按图3-1专用口径重写并新建候选图
   - 现状：用户指出此前重画的 `图3-1` 没有严格按 `E:\bishe\codexlunwen\assets\mermaid\黑白H图功能模块图提示词与绘制规范.md` 的黑白 H 图模板执行，要求重新使用该模板，先把本次实际要用的图3-1提示词写进去，再重新调用工具出图，且不覆盖原图。
   - 处理方向：已在黑白 H 图规范文件中补入“图3-1 系统总体功能结构图”专用提示词和二次修正提示词；随后按“总框 + 一级功能竖框 + 下接二级功能竖框”的三层结构重画候选图，保留当前版本 `fig3-1-h-candidate-f.png` 供用户确认，未插回 Word。
149. [x] 副本(2)长表续表标题补入并回正第5章残留测试表题
   - 现状：用户要求继续复查 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx`，并参照示例图为跨页长表补入“表X ……（续）”标题；同时文档中第 `5.2` 仍残留旧测试表题与“其它”字样未完全收口。
   - 处理方向：已对数据库设计区的长表按分页续表思路拆分并补入红色“（续）”标题，同时继续按“旧字删除线 + 新字红字”方式修正 `表5-2`、`表5-3`、`表5-4` 的测试表题和 `5.2.5` 段中的规范用词。
148. [x] 副本(2)文档图表引用、章节标题与病句残留继续按审阅痕迹修订
   - 现状：用户要求在 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx` 中继续处理复查后剩余的明确问题，但仍保持“旧字删除线、新字红字”的可视化修改方式；同时用户说明第 `3.3.2` 节表号格式暂不处理，参考文献区也不按 DOI 补写。
   - 处理方向：已定点修正 `3.2.5` 小节标题、研究方法病句与编号风格、`JAVA语言介绍` 标题、`结合系统实际实现` 等口吻问题，以及第 2 章和第 5 章多处 `如图/如表...所示：` 的标点格式残留。
147. [x] 副本(2)文档重点段落按审阅痕迹标红与删除线
   - 现状：用户要求对 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现 - 副本 (2).docx` 中已识别出的高风险段落直接做可视化修订，不改图号序号；新改内容需显示为红色字体，被替换旧句需保留并加删除线，便于后续在 Word 中继续人工复核。
   - 处理方向：已定点处理摘要、`1.3.2 研究方法`、`5.1 测试目的及方法`、`5.3 测试结论` 和“结论”段落，将旧表述保留为删除线，并在同段后追加红色修订文本。
146. [x] 副本数据库修正版除综述外关键章节补充文献引用
   - 现状：用户指出主论文不应只有 `1.2 国内外文献综述` 才出现参考文献编号，`1.3`、`1.4`、第 2 章和第 3 章的技术选型、需求分析、结构设计等段落也需要有文献支撑，否则整体论文会显得“只有综述部分像论文”。
   - 处理方向：已在不破坏正文流畅性的前提下，为研究内容与方法、相关技术介绍、技术可行性、功能与性能需求、系统结构设计、登录与权限设计、数据库概念设计等关键段落补入对应参考文献编号。
145. [x] 论文黑白 H 图功能模块图提示词规范沉淀到图片目录
   - 现状：用户要求将当前可复用的黑白 H 图绘制方法、提示词和修正技巧沉淀为独立说明文件，保存到论文图片目录中，便于后续继续生成同风格模块图时直接复用。
   - 处理方向：已在 `E:\bishe\codexlunwen\assets\mermaid` 下新增黑白 H 图提示词规范文档，内容包含通用提示词模板、项目专用提示词、Next AI Drawio 使用技巧和降级手工绘制规则。
144. [x] 副本数据库修正版第 3 章模块图改黑白 H 图并回正 3.1 论文口吻
   - 现状：用户进一步指出 `3.1 系统结构设计` 中“结合当前系统真实实现”这类表述不符合论文书面语，同时第 3 章 `图3-3`、`图3-4`、`图3-5` 的功能模块图仍是默认 Mermaid 风格彩色框图，不符合“黑白 H 图”的交付要求。
   - 处理方向：已将 `3.1` 与第 3 章相关段落改为更规范的论文表达，并在 `E:\bishe\codexlunwen\assets\mermaid` 中重绘管理员、部门经理、员工三张黑白 H 图后重新替换回 Word 正文。
143. [x] 副本数据库修正版第 3 章结构设计图回嵌与数据库表前说明去英文
   - 现状：`基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 的第 3 章此前存在两类问题：一是 `图3-1` 至 `图3-8` 只有图题没有实际插图，`3.1` 文字也偏泛化，和已生成的设计图、真实系统模块对应不够紧；二是 `3.3.2` 各表前说明仍保留 `t_user`、`t_department` 等英文表名，且 `如表 1 所示` 写法存在多余空格。
   - 处理方向：已基于 `E:\bishe\codexlunwen\assets\mermaid` 重新回嵌第 3 章设计图，重写 `3.1` 与 `3.2.1` 的相关表述，补齐 `图3-8 数据库E-R图` 图号；同步删除数据库逻辑设计表前说明中的英文表名，并统一改为 `如表1所示。` 的紧凑写法。
142. [x] 副本数据库修正版第 4 章截图回嵌与图表引用格式统一
   - 现状：`基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 的第 4 章此前只嵌入了前 13 张截图，且存在按文件编号直接套用导致“图题与截图内容不一致”的问题；同时正文中的“如图 / 如表”引用仍混有多余空格和冒号写法，不符合用户要求的 `如图4-9所示。`、`如表5-2所示。` 口径。
   - 处理方向：已基于 `E:\bishe\antigravitylunwen\论文图片集` 重新映射并回嵌第 4 章 `图4-1` 至 `图4-26` 的对应截图，补齐缺失图片并回正错配图题；同步统一图题、表题与正文图表引用格式，改为连字符编号且去掉多余空格。
141. [x] 副本数据库修正版第 4 章关键口径校正
   - 现状：`基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 的第 4 章仍存在少量事实与表达偏差，包括“三层架构”措辞不准确、薪资管理段落中的个人五险一金比例与真实代码不一致，以及 `4.2.5 系统管理` 对配图支撑范围说明不足。
   - 处理方向：已定点修正 `4.1` 章开头描述、`4.2.2` 中五险一金个人比例口径，并重写 `4.2.5` 两段说明，明确图 4-24 至图 4-26 的代表性页面范围，同时补足门户配置实际落地为本地 `sys-config.json` 文件的事实口径。
140. [x] 副本数据库修正版第 4 章前端实现表述去重
   - 现状：`基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 的 `4.1.4` 至 `4.1.7` 存在前端小节提前讲后端状态流转、考勤绩效界面与后端规则口径交叉、`个人中心` 与 `密码修改` 内容重复的问题。
   - 处理方向：已定点改写 `4.1.4`、`4.1.5`、`4.1.6`、`4.1.7` 的正文，使前端部分聚焦页面布局、操作入口和交互反馈，并将 `个人中心` 与 `密码修改` 拆分为资料维护入口和账户安全入口两段独立表述。
139. [x] 副本数据库修正版第 5 章测试用例回正
   - 现状：`基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 的 `5.1` 与 `5.2` 存在测试范围表述偏大、表头与内容错位、旧菜单名称残留以及测试项与真实业务闭环不完全一致的问题。
   - 处理方向：已定点重写 `5.1`、`5.2.1` 至 `5.2.5` 和 `5.3`，补齐登录校验、工号唯一性、薪资审核发放链路、考勤导入汇总更新与角色菜单跳转等测试项，并将旧“个人空间管理/公司公告管理”等词替换为当前系统真实菜单口径。
138. [x] 副本文档第 2.2.1 节、5.3 节与“结论”段落继续回正
   - 现状：在前几轮完成 `1.3`、`3.x`、`4.2` 和 `5.2` 后，副本文档中 `2.2.1 功能需求分析` 仍偏旧版功能清单式写法，且残留“匿名留言”等与当前系统不符的表述；`5.3 测试结论` 与最后“结论”两段则仍偏套话，缺少针对当前系统实现结果的收束。
   - 处理方向：在 `基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 上继续定点修改，重写 `2.2.1` 为按真实角色和业务闭环展开的需求分析，重写 `5.3` 测试结论与最终“结论”两段，使其更贴合当前系统实现与测试结果。

137. [x] 副本文档第 1.3 节与第 3.1/3.2 节按当前系统口径补厚
   - 现状：上一轮已完成数据库章节和 `4.2/5.2` 的回正，但 `1.3 研究的主要内容和方法` 仍保留旧“两角色”写法，`3.1 系统结构设计` 内容偏少，`3.2 功能模块设计` 仍残留旧打卡与留言口径，且部门经理、员工、核心流程三个子模块说明缺失或顺序异常。
   - 处理方向：在 `基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 上继续定点修改，重写 `1.3.1`、`1.3.2` 的正文表述，补回管理员/部门经理/员工三角色口径；扩写 `3.1` 的 B/S 协作关系；重写并补齐 `3.2.1` 至 `3.2.5` 的模块设计说明，恢复正确的章节顺序与图号引用。

136. [x] 副本文档第 4.2 节后端实现重写并回正第 5.2 节测试表题
   - 现状：数据库章节修正版虽然已完成，但 `4.2 系统后端功能的实现` 仍明显偏模板化，未充分体现当前系统中员工账号创建、考勤导入汇总、绩效联动薪资重算、工资五态流转和门户配置持久化等真实实现；同时 `5.2` 中仍残留“客户关系管理系统登录界面”的错误测试表题，且第 4 章图号存在重复跳号。
   - 处理方向：在 `基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx` 上继续定点修改，重写 `4.2` 的 5 个后端功能小节及总述，按真实代码逻辑补厚正文；同步修正 `5.2.3`、`5.2.4` 等测试表题，并将第 4 章图号统一续接到 `图4-26`。

135. [x] 副本文档数据库章节按真实库重做并输出修正版 Word
   - 现状：上一轮已完成 `副本文档问题清单与数据库核对报告`，并确认 `基于Java的员工薪资管理系统设计与实现 - 副本.docx` 中 `3.3.1` 仍保留“留言、工作日记、请销假”等旧稿口径，`3.3.2` 只列出 17 张表，且缺少真实存在的 `t_position`。
   - 处理方向：本轮不覆盖原始副本，而是以其为源文件，结合真实数据库 `bishe` 的 `information_schema` 表结构，重写 `3.3.1 数据库概念设计`、`3.3.2 数据库逻辑设计` 的 18 张表说明段，并新增岗位表，输出修正版 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现 - 副本-数据库修正版.docx`。

134. [x] 副本文档问题清单与数据库一致性核对报告输出
   - 现状：用户要求不要直接改 `基于Java的员工薪资管理系统设计与实现 - 副本.docx`，而是先单独输出一份“需要修改的地方 + 数据库表是否与真实系统一致”的文档。
   - 处理方向：本轮只读副本文档，提炼出正文偏薄和旧稿残留部分；同时连接真实数据库 `bishe`，先查表结构，再抽查各表前 5 条数据，最终生成单独报告 `E:\bishe\codexlunwen\副本文档问题清单与数据库核对报告.md` 与 `E:\bishe\codexlunwen\副本文档问题清单与数据库核对报告.docx`。

133. [x] 基于本地 PDF 摘要重写国内外文献综述
   - 现状：用户指出当前 `1.2 国内外文献综述` 仍然偏概括，要求参考给出的模板写法，直接读取已经下载的 PDF，从研究目的、实现内容和结论层面组织综述，而不是停留在空泛概述。
   - 处理方向：本轮直接读取 `E:\bishe\论文要求` 目录下正式采用文献的 PDF 摘要/引言，提炼各篇文献的研究目的、系统模块、实现路径与主要结论，按更接近样文的综述方式重写 `1.2`，并按原正文样式回写主论文 `docx`。

132. [x] 文献综述句式去模板化并回正正文字体
   - 现状：用户进一步指出 `1.2 国内外文献综述` 仍有明显的“按年份机械起段”痕迹，同时主论文中这一节正文的字体表现与参考论文不一致，存在新生成段落字符格式丢失的问题。
   - 处理方向：本轮重写 `1.2` 的段首句式，改成更自然的综述表达；同时以 `1.1.2` 正文段落为参照，重建 `1.2` 的 7 个正文段，统一回正为中文宋体、西文 Times New Roman、五号字的正文字符格式。

131. [x] 文献综述润色版正式覆盖回主论文 Word 成品
   - 现状：上一轮已生成 `基于Java的员工薪资管理系统设计与实现-文献综述润色版.docx`，但由于原主论文文件被 Word 占用，尚未正式覆盖回主文件。
   - 处理方向：在用户确认关闭 Word 后，将润色版副本正式覆盖回 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现.docx`，并复查 `1.2` 段落数量、参考文献条数及文档主体结构。

130. [x] 文献综述口吻去 AI 化并导出润色版 Word 副本
   - 现状：用户指出 `1.2 国内外文献综述` 当前写法过于模板化，尤其是“结合本地已下载文献，本文不再按……”这类元话语痕迹较重，不像正常本科论文表达。
   - 处理方向：本轮将 `论文正文草稿.md` 中 `1.2` 改写为更平实的综述口吻，删除明显 AI 化和自我说明式表达；由于主论文 `docx` 正被外部程序占用，暂输出同内容的润色版副本 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现-文献综述润色版.docx`。

129. [x] 文献综述口径回正为中英文混排时间序并复核本地 PDF 覆盖
   - 现状：用户进一步说明“不是国内外分开写，而是中英文混搭，只按时间排序”；同时要求确认当前正式采用文献是否还缺本地 PDF。
   - 处理方向：本轮复核 `E:\bishe\论文要求` 目录，确认当前 21 篇正式采用文献均已有本地 PDF；同步将 `论文正文草稿.md` 中 `1.2 国内外文献综述` 改为中英文混排、纯按年份倒序展开，并定点回写主论文 `docx`。

128. [x] 论文参考文献与国内外综述按时间倒序重排并重新同步主论文
   - 现状：用户进一步明确要求参考文献列表必须严格按“从新到老”排序，且 `1.2 国内外文献综述` 中的国内、国外研究梳理也都要按时间倒序展开；此前 `论文正文草稿.md` 与 `文献池清单.md` 已基本完成调整，但 `参考文献核查表.md` 和主论文 `docx` 仍停留在旧顺序。
   - 处理方向：本轮以 `论文正文草稿.md` 当前版本为唯一基准，同步回正 `参考文献核查表.md` 的编号顺序，并将倒序版 `1.2 国内外文献综述` 与 21 篇正式参考文献定点回写到主论文 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现.docx`，随后复查边界段落与条目数量。

127. [x] 主论文 docx 同步回写本地 PDF 终选参考文献与 1.2 综述
   - 现状：上一轮已完成 `论文正文草稿.md` 中 `1.2 国内外文献综述` 与参考文献区的重写，但尚未把改动同步回主论文 Word 成品 `E:\bishe\codexlunwen\基于Java的员工薪资管理系统设计与实现.docx`。
   - 处理方向：本轮直接基于现有 Markdown 终稿内容，对主论文 `docx` 做定点替换，仅修改 `1.2 国内外文献综述` 正文区和“参考文献”区；完成后复查段落边界、参考文献条数和 `致谢` 位置，确保不影响其它章节、图片和表格。

126. [x] 论文参考文献改为本地 PDF 终选集并回写国内外研究现状
   - 现状：用户要求不再继续混用“在线候选但未下载原文”的条目，而是直接以 `E:\bishe\论文要求` 目录下已经存在的 PDF 为准，按“近五年内，2020 年也可，但优先强相关”重新筛选正式参考文献，并同步改写论文 `1.2 国内外研究现状`。
   - 处理方向：本轮将正式参考文献压缩为 21 篇本地 PDF 终选条目，去掉档案服务转向、移动端边缘条目和未落地原文候选；同步重写 `论文正文草稿.md` 中 `1.2 国内外文献综述`，并将 `文献池清单.md`、`参考文献核查表.md` 一并收口到同一套本地可核口径。

125. [x] 论文参考文献全量压到近五年并补做 PDF 缺口盘点
   - 现状：用户明确要求参考文献必须全部控制在近五年内，同时已将当前手头论文 PDF 统一放到 `E:\bishe\论文要求`，需要顺手核对哪些已下载、哪些仍缺本地 PDF。
   - 处理方向：本轮继续仅调整 `论文正文草稿.md` 末尾参考文献区、`文献池清单.md` 和 `参考文献核查表.md`，将残留的 `2015/2016/2019/2020` 条目全部替换为 `2021-2025` 新条目；同步把 `施春雷 / 姚佰允 / 马佼 / 袁筱钰` 等已下载 PDF 条目改为已核对状态，并在核查表中明确新补 8 条在线文献仍待下载 PDF。

124. [x] 论文参考文献再清洗：删除查不到、重复和弱相关条目
   - 现状：用户进一步指出当前参考文献中仍有 3 类问题：一是 `[7]` 所指外文条目在实际检索中对不上；二是 `人力薪资管理系统V1.0` 只有单页成果简介且下载受限；三是 `[21]` 铁路视频系统与论文主题相关性过弱；此外 `[18]` 与 `[22]` 还出现了重复的 `施春雷` 条目。
   - 处理方向：本轮继续仅调整参考文献区、文献池与核查表，将查不到、重复和弱相关条目替换为更贴近论文主题且更容易后续补 PDF 的候选文献，同时暂不改 `1.2 国内外文献综述` 正文。

123. [x] 论文参考文献候选条目先行调换，正文综述暂缓
   - 现状：用户已指定 3 条新的候选参考文献（`施春雷 2022`、`姚佰允等 2025`、`袁筱钰等 2022`），要求先替换进参考文献列表和文献池，但暂时不要改 `1.2 国内外文献综述` 的正文论述，待后续下载 PDF 后再按原文内容回调综述表述。
   - 处理方向：先仅调整 `论文正文草稿.md` 末尾参考文献区、`文献池清单.md` 与 `参考文献核查表.md` 中对应条目；后续待用户补齐 PDF 后，再按新文献内容同步改正文综述中的作者提法与引用逻辑。

122. [x] 论文参考文献按本地 PDF 逐篇核查并补充来源网站
   - 现状：主论文正文与文献池中多条参考文献存在年份、文献类型、作者或期刊信息写错的问题，例如把期刊论文误写成学位论文、把 2015/2016/2019/2022 年学位论文整体写成了 2023/2024/2025 年；同时文献池缺少“来源网站”字段，后续难以回到原站补正式引用。
   - 处理方向：基于 `E:\bishe\论文要求` 目录下本地 PDF、`毕业设计开题报告.txt` 以及期刊官网/DOI 入口，逐条核查 24 篇正式参考文献；同步修正 `论文正文草稿.md`、`文献池清单.md`，并新增 `参考文献核查表.md` 标注对应来源网站与待二次点验条目。

121. [x] 网站成图输入文件整理到图片目录
   - 现状：用户改为自行使用 `https://generator.cengxuyuan.cn/` 生成论文配图，不再要求继续强行自动化点击会员拦截页面，而是要求直接提供该网站可粘贴的原始输入数据，例如流程图节点/连线文本、用例图文本和 SQL 转 ER 图脚本。
   - 处理方向：已在 `E:\bishe\codexlunwen\assets\mermaid\generator-inputs` 下整理 `README.md`、功能模块图/用例图输入文本、两张流程图的节点与连线文本，以及裁剪后的核心表 `SQL`，便于用户手动粘贴生成图片。

120. [x] 论文长表改回单表并为全部表格保留重复标题行
   - 现状：用户明确要求数据库逻辑设计部分只需要 Word 的“重复标题行”，不要再把长表强行拆成“表3.x ……（续）”续表；此前补的 5 个续表标题和续表结构反而破坏了原本的一张表表达。
   - 处理方向：已将主论文 `docx` 中 `表3.2/3.4/3.5/3.6/3.8` 的续表数据行并回原表，删除“（续）”标题与对应断表，只保留各表首行 `tblHeader` 重复标题行标记，恢复为单表跨页自动重现表头。

119. [x] 论文表前说明行字体、表格粗横线与未实现功能表述回正
   - 现状：用户继续指出主论文中第 3.3.2 节新补的“（1）…如表3.1所示”说明行字体与样例不一致；数据库表格未做出样例论文那种“表头上边/表头下边/整表底边”三道粗横线；第 4.5.3 节未明确写出社保比例；第 5 章与正文中还残留“Excel 打卡自动判定迟到早退”的表述，但该功能实际并未完成。
   - 处理方向：单独输出一份版式修订版 `docx`，为表前说明行显式补齐宋体/Times New Roman 字体，统一加粗表格关键横线，补写社保扣缴比例，并同步删除或降级所有超出现实实现的“自动判定迟到早退/加班”描述。
   - 同步结果：在用户关闭 Word 占用后，已将 `基于Java的员工薪资管理系统设计与实现-版式修订.docx` 正式回写覆盖到主论文 `基于Java的员工薪资管理系统设计与实现.docx`。

118. [x] 论文正文英文实现名残留、表前说明行缺失与测试章节结构回正
   - 现状：用户指出主论文 `codexlunwen` 版中第 4 章仍残留 `AuthController`、`AttendanceServiceImpl`、`SalaryServiceImpl` 等英文实现名词，Word 中出现蓝色语法提示线；第 3.3.2 节各数据表前缺少样例论文那种“某某表，存储……如表3-x所示”的说明行；第 5 章测试结构也与样例论文差距较大，存在“测试环境/性能测试”等项目文档式标题，不够规范。
   - 处理方向：直接修订主论文 `docx`，将第 4 章实现描述统一改成中文论文口吻；为表 3.1~3.8 逐一补齐表前说明行并同步规范表题；同时把第 5 章调整为“测试目的及方法 + 功能测试 + 测试结论”的样例式结构，并重写两张测试表标题与内容。

116. [x] 论文主文档回嵌第 3 章设计图与第 4 章现有系统截图
   - 现状：用户确认第 3 章 PNG 成图可用后，要求将 `E:\bishe\codexlunwen\assets\mermaid` 中的论文设计图正式插回主论文，同时第 4 章系统截图只允许从 `E:\bishe\antigravitylunwen\论文图片集` 中选用，不再混用其它目录。
   - 处理方向：已为 `论文正文草稿.md` 补齐第 4 章正式图题、清理当前缺图占位，并新建合并版 `combined-image-map.json`，将第 3 章设计图与第 4 章现有系统截图一起回嵌到主论文 `docx`。

117. [ ] 论文第 4 章仍缺的经理端与门户详情截图待补截
   - 现状：对照 `论文正文草稿.md` 第 4 章与 `E:\bishe\antigravitylunwen\论文图片集` 现有图片后，管理员公告管理页、经理个人中心/考勤审批/薪资核算草稿页、员工请假加班申请页、公告详情页与“关于我们”详情页等截图仍未齐。
   - 处理方向：已在 `E:\bishe\codexlunwen\系统截图缺口清单.md` 记录缺口；待用户后续补截后，再把这些截图继续插回论文对应小节。

115. [x] 员工端“工资个税与社保”列表首列改回账期月份
   - 现状：员工端“我的个税缴纳与社保档案”列表第一列错误显示为“汇算清缴年度”，导致同一年度多条记录都只显示 `2026/2025`，用户无法直接看出对应的是哪一个月份账期。
   - 处理方向：前端列表首列改为显示 `yearMonth`，标签统一为“账期月份”；后端分页排序同步改为按 `year_month` 倒序，保证员工端按月份查看个税累计记录。

114. [x] 管理员薪资核算页“批量驳回”按钮样式回正
   - 现状：管理员端薪资核算页新增“批量驳回”后，按钮同时叠加了 `type="danger"` 与 `plain`，与当前页面统一的渐变按钮皮肤冲突，出现默认文字发灰、鼠标悬浮才变白的反色问题。
   - 处理方向：移除该按钮的 `plain` 样式，统一回到管理员页现有危险操作按钮的实心渐变风格，避免继续与 Element Plus 的浅色按钮状态打架。

113. [x] 经理页补齐“经理本人可见/可选”并修正张伟名下工资单 manager_id 脏数据
   - 现状：用户反馈登录 `10001/张伟` 后，经理页“添加草稿”下拉无数据，薪资核算列表又几乎只剩 `2025-01`。排查发现一方面经理页查询代码仍排除了经理本人；另一方面张伟名下 `2026-01`~`2026-03` 的工资单虽然 `manager_no/manager_name` 已是张伟，但 `manager_id` 被错误写成了 `1`（管理员）而不是 `101`。
   - 处理方向：后端经理薪资查询不再排除经理本人；经理批量生成草稿时把本人一起纳入；前端员工选择缓存中强制补入当前经理本人；同时直接修正张伟名下工资单的 `manager_id` 脏数据，使经理页能按当前口径正确显示近月数据。

112. [x] 部门数量与经理归属修正：新增第 5 个部门并让刘洋独立挂部门
   - 现状：数据库中只有 4 个部门，但存在 5 个经理账号；`10005/刘洋` 既是经理账号，又被挂在 `技术研发部` 且 `manager_id=101(张伟)`，导致界面上出现“技术研发部经理明明是张伟，但刘洋也混在技术研发部”的错误展示。
   - 处理方向：新增第 5 个部门并指定 `10005/刘洋` 为该部门经理；同步将刘洋员工档案与历史工资单中的部门/经理信息改到新部门，确保张伟继续只管理技术研发部。

111. [x] 薪资审核补齐“驳回退回经理修改”链路
   - 现状：图 3-7 中已包含“管理员审核不通过 -> 退回经理修改”的业务分支，但系统真实实现只有“待审核 -> 已审核 -> 已发放”，缺少驳回接口、前端按钮和重提审核入口，导致程序与论文流程图不一致。
   - 处理方向：新增管理员单笔/批量驳回接口，将 `2=待审核` 退回为 `5=已驳回`；前端补“驳回/批量驳回”按钮、状态标签与筛选项；同时允许经理对“已驳回”记录继续编辑并重新提交审核。

110. [x] 薪资核算流程图右侧“继续编辑草稿”回路改为完整正交连通路径
   - 现状：用户开始单独检查 `07` 流程图，并继续要求右侧“否 -> 继续编辑草稿 -> 经理编辑个别草稿”回路画成真正连通的横平竖直箭头，不要看起来像断开的折线。
   - 处理方向：继续微调 `07-图3-7-薪资核算与审核发放流程图.png`，将右侧否分支改为“水平到框下方、竖直到继续编辑草稿、再水平返回经理编辑个别草稿”的完整正交路径。

109. [x] 登录流程图失败回路继续收敛为单拐点路径
   - 现状：用户继续指出 `06` 登录流程图中左右失败回路仍存在多余折返，希望在提示框下方只保留一个拐点后再进入提示框。
   - 处理方向：继续压缩 `06-图3-6-登录与角色分流流程图.png` 的左右失败分支，改成“判断框横向到提示框下方，再单次上拐进入提示框”的单拐点路径。

108. [x] 登录流程图失败分支改为“经提示框再返回输入框”的两段式路径
   - 现状：用户继续要求 `06` 登录流程图中的失败分支不要直接贴着提示框侧边进入，而应先走到提示框下方，再进入提示框，最后再从提示框水平返回输入框。
   - 处理方向：继续微调 `06-图3-6-登录与角色分流流程图.png` 的左右失败回路，使其分为“判断框 -> 提示框”和“提示框 -> 输入框”两段路径，贴合用户手绘示意。

107. [x] 登录流程图提示框与输入框改为同层水平回流
   - 现状：用户继续指出 `06` 登录流程图中左右提示框虽然已回接输入框，但提示框本体仍未与输入框处于同一水平层，视觉上仍不够“直着过来”。
   - 处理方向：将左右提示框整体上移到与输入框同一水平线，并保留从提示框直接水平箭头返回输入框的表达。

106. [x] 论文流程图 6/7 二次重排：修复顶部箭头重叠与右侧回流折线
   - 现状：用户继续指出 `06` 登录流程图顶部进入输入框处存在箭头重叠不清的问题，同时 `07` 薪资核算流程图右侧“继续编辑草稿”回流到“经理编辑个别草稿”的线不应折弯绕回。
   - 处理方向：重排两张流程图布局，`06` 将失败回流改接到输入框左右侧，避开顶部主箭头；`07` 将“继续编辑草稿”提升到与“经理编辑个别草稿”同一横向层级，并同步修正左下“退回经理修改”分支的回流表达。

105. [x] 论文流程图 6/7 改为横平竖直箭头版
   - 现状：用户要求 `06-图3-6-登录与角色分流流程图.png` 与 `07-图3-7-薪资核算与审核发放流程图.png` 按正式论文风格继续优化，连线必须横平竖直且带箭头。
   - 处理方向：继续微调绘图脚本，将两张流程图中失败回路、分支回流与主流程链统一改为正交流程箭头，避免继续出现无箭头折线或斜向回路。

103. [x] 论文 ER 图基数纠正：员工对考勤/绩效为一对多，工资单对反馈按论文口径收为一对一
   - 现状：用户指出数据库总体 ER 图中的两个基数关系有误：一个员工应对应多条考勤记录；论文口径下单条工资记录只对应一条薪资反馈。
   - 处理方向：修正 `08-图3-8-数据库总体-ER-图.png` 中 `员工-考勤记录`、`员工-绩效记录`、`薪资记录-薪资反馈` 三处基数标注，使图面表达与当前论文业务口径一致。

104. [ ] 后端“工资单一单一反馈”约束待与论文口径对齐
   - 现状：论文 ER 图现已按“一条工资记录对应一条薪资反馈”展示，但当前数据库表 `t_salary_feedback` 仅通过 `salary_record_id` 普通外键关联工资单，未看到唯一约束，系统层面仍可能产生一条工资单对应多条反馈。
   - 处理方向：后续如需让系统实现与论文图完全一致，需要补查后端提交反馈逻辑，并考虑为 `salary_record_id` 增加唯一约束或服务层幂等校验。

102. [x] 论文 ER 图继续上移员工实体并加大与薪资记录的竖向间距
   - 现状：用户继续指出 `员工` 方框仍不够高，且与 `薪资记录` 的垂直距离仍偏近，希望进一步上移员工实体并拉开上下两块的留白。
   - 处理方向：继续微调 `08-图3-8-数据库总体-ER-图.png` 的中心布局，上移 `员工` 方框本体与其上下属性区，下移 `生成` 到 `薪资记录` 的竖向链路，增加两者之间的视觉间距。

101. [x] 论文参考文献扩充至24篇并上移 ER 图中的员工实体
   - 现状：用户明确要求将参考文献总数从 20 篇改为 24 篇，同时保持外文最多 4 篇；并要求数据库总体 ER 图中的 `员工` 实体整体上移，使中心布局更紧凑。
   - 处理方向：在保留原 4 篇外文的前提下，为 `文献池清单.md` 与 `论文正文草稿.md` 新增 4 篇中文参考文献；同步上移 `08-图3-8-数据库总体-ER-图.png` 中 `员工` 实体及其上下关联元素的位置。

100. [x] 论文 ER 图中心实体与关系菱形改为单段直线连接
   - 现状：用户进一步要求数据库总体 ER 图中 `员工` 实体与上方/左右关系菱形之间不要再出现折线，两个部件之间必须使用单段直线连接。
   - 处理方向：继续微调 `08-图3-8-数据库总体-ER-图.png` 的中心布局，保留 `员工` 居中和属性上下分流，但将 `绑定`、`归属`、`形成`、`参与` 到 `员工` 的连线统一改成单段直线。

99. [x] 论文 ER 图属性分流重排与参考文献外文比例收紧
   - 现状：用户继续指出数据库总体 ER 图中“员工”“薪资记录”两个实体的属性区不应全部堆在同一侧，要求改成上下分流以避免连接线压住属性；同时明确要求参考文献不要保留过多外文，国外文献最多 4 篇，总量维持 20 篇左右即可。
   - 处理方向：将 `08-图3-8-数据库总体-ER-图.png` 中 `员工` 改为上 3 下 2、`薪资记录` 改为上下分布，并重新整理 `文献池清单.md` 与 `论文正文草稿.md` 的参考文献结构，压缩外文为 4 篇、中文为主。

98. [x] 论文图表三次微调：权限结构图改 H 图，ER 图补足核心属性
   - 现状：用户确认图 3.1 的 H 图风格可保留，并继续要求图 3.2 也改成 H 图表达；同时指出数据库总体 ER 图属性过少，希望更贴近系统真实表结构。
   - 处理方向：将 `02-图3-2-权限与角色结构图.png` 调整为与图 3.1 同风格的 H 图；将 `08-图3-8-数据库总体-ER-图.png` 扩充为核心属性增强版，按真实数据库字段补充账号、员工、考勤、绩效、薪资、公告、反馈等实体的关键属性，但本轮仍只更新 PNG，不回嵌 Word。

97. [x] 论文图确认前暂停回嵌主文档，仅迭代 PNG 成图
   - 现状：用户已确认 H 图可以保留，但要求后续图片在其确认“可以了”之前，先只改 PNG，不要继续放入论文主文档。
   - 处理方向：后续图表迭代阶段仅更新 `codexlunwen/assets/mermaid/*.png` 与绘图脚本，不再自动重建主论文 `docx`；待用户确认整套图片通过后，再统一嵌回 Word。

96. [x] 论文图表二次修正：功能模块图竖排、用例图去框、登录流程序化、ER 图原创化
   - 现状：上一版虽然已按论文图范式重做，但用户继续指出 4 个具体问题：功能模块图子框仍不是样例中的竖排窄框样式；用例图不需要系统边界框；登录流程图多了“管理员登录”框；ER 图仍容易被误认为沿用了用户样图结构。
   - 处理方向：继续定点修改绘图脚本，将功能模块图改为一级/二级模块均使用竖排窄框，用例图去掉演员名方框与系统边界框，登录流程图从“开始”直接进入输入框，并按系统真实业务重新组织原创 ER 图后重新嵌回论文。

95. [x] 论文第3章图表按样例重做为标准论文图范式
   - 现状：上一轮虽然已将第 3 章图替换进正文，但整体仍偏 Mermaid 工程示意图，不符合用户给出的样例范式；尤其功能模块图、用例图、流程图和 ER 图的图形元素与版式都不够像正式论文配图。
   - 处理方向：放弃继续微调 Mermaid 出图，直接改为手工绘制黑白论文图，统一采用树状矩形功能模块图、标准 UML 用例图、标准流程图和 Chen 风格 ER 图，并重新覆盖 PNG 后回嵌主论文 docx。

94. [x] 论文第3章 UML 图、功能模块图与 ER 图重绘并回嵌正文
   - 现状：原论文第 3 章虽然已有 Mermaid 图，但角色用例图不够像标准 UML，用例演员标签也不够规范，数据库 ER 图信息量偏少，功能模块图的层次表达也不够正式。
   - 处理方向：按系统真实功能重绘图 3.1、图 3.3、图 3.4、图 3.5、图 3.8，统一为黑白论文风格，并重新生成主论文 `docx` 与图源码附件，确保图源码、PNG 和正文 Word 同步更新。

93. [x] 图表绘制 MCP 安装与多 Agent 共享接入
   - 现状：当前 Codex 只内置了 Playwright、MySQL 等 MCP，缺少适合绘制 UML、功能模块图、流程图、ER 图的专用图形工具。
   - 处理方向：安装并接入 `plantuml-mcp-server` 与 `mcp-mermaid`，写入用户级 `C:\Users\614377781\.codex\config.toml`，并在工作区补充 `diagram-mcp` 技能说明，方便后续其他 agent 直接复用。

92. [x] 论文终稿、图源码附件与参考文献清单交付
   - 现状：`codexlunwen` 目录下已完成论文中间产物，但还未真正输出可提交的主论文 `.docx`、图源码附件 `.docx` 和参考文献规范化 `.xlsx` 成品。
   - 处理方向：基于已冻结的系统事实、样式分析、图表清单和文献池，生成正式论文成品文件，补齐图源码附件与参考文献清单，并将交付结果回写状态文件，避免论文任务只停留在 Markdown 草稿阶段。
   - 补充说明：已继续修复样式抽取误带来的正文段前分页问题，重新输出“分页修复带图版”论文，避免 Word 中出现大段空白和图文被强制拆页。
   - 二次修订：已按论文口吻修正中文摘要措辞，去掉摘要中的“了”字；同时将第 3 章图表重新调整为 HIPO 功能图、角色用例图和标准流程图风格，并重新渲染输出新的论文修正版。
   - 三次修订：已继续将第 3 章系统图统一优化为黑白极简风格，并通过 Mermaid `linear + elk` 布局尽量收敛为直线、正交连线，新增输出 `thesis-figure-bw-straight.docx` 作为当前最新图表修正版。

91. [x] 经理个人中心资料空白与员工端按钮类型警告修复
   - 现状：部门经理账号进入共享后台后，个人中心中的姓名、性别、手机、身份证号、部门、银行卡号、岗位、入职时间等字段全部显示为 `-`；同时员工端修改密码按钮写成了 `type=" primary"`，Element Plus 控制台持续报按钮类型校验 warning。
   - 处理方向：修复 `admin/index.html` 中 `loadSelfInfo()` 已损坏的取数逻辑，统一用 `/api/auth/me` 与当前登录态回填 `selfInfo/selfEditForm`，保证经理个人中心可正常显示和编辑本人档案；同步修正 `home.html` 按钮 `type` 的多余空格，消除员工端控制台警告。

90. [x] 管理员端考勤/薪资核算/薪资发放表格未铺满修复
   - 现状：管理员端“考勤数据”“薪资核算”“薪资发放”三个列表页因为列几乎全部使用固定 `width`，导致表格右侧出现大块留白，未能铺满卡片容器。
   - 处理方向：保留原有列顺序与交互，只将这三张表中的部分固定列宽调整为 `min-width`，给 Element Plus 留出可伸缩列，让表格随容器宽度自动拉伸铺满。


89. [x] 扩展外部技能库：集成论文撰写与知网搜索插件
   - 实施内容：从 GitHub 克隆了 lunwen-skill、cnki-skills、gs-skills 三个专业组件到 e:\bishe\.agents\skills，为后续多 Agent 协同撰写毕业论文和检索学术资料提供了底层 SOP 支持。

87. [x] 2026-03 薪资/绩效/考勤/发放重复脏数据清理与引用回绑
   - 现状：`2026-03` 同月存在两套员工工资单、绩效记录、考勤记录；发放表金额与工资表 `net_salary` 大面积不一致，且薪资反馈引用了即将清理的旧工资单 ID。
   - 处理方向：保留后一套自洽的 3 月数据，删除旧重复工资/绩效/考勤记录；同步修正经理工资单、发放记录金额与反馈表 `salary_record_id` 引用，确保 3 月只保留 15 条一致数据。

88. [x] 2026-01/2026-02 经理工资与个税累计链路待补齐
   - 现状：`2026-01` 已补齐 15 条工资单，但 `2026-02` 仍缺经理账号 `10001`-`10005` 的工资记录；同时 `t_tax_accumulate` 在 2026 年 1-3 月仅残留少量旧记录，和当前工资表链路不一致。
   - 处理方向：直接复用已存在的 2026-02 经理绩效/考勤数据补齐缺失工资单，并按当前工资表重建 2026 年 1-3 月的累计个税记录，同步补齐 1、2 月发放记录，确保工资、支付、个税三张表口径一致。

86. [x] 管理员页薪资反馈列表头像兜底修复
   - 现状：管理员端“薪资反馈”列表最后几条记录未显示真实头像，只回退到首字母头像；问题集中在工号 `10001`、`10003`、`10004` 等记录。
   - 处理方向：统一薪资反馈列表的员工资料解析链，改为复用 `resolveEmployeeProfile(...)` 与 `getStoredAvatar(...)`，让员工、经理账号都能按工号/账号正确回填头像、姓名与工号。

85. [x] 2026-03 绩效记录补齐并回写工资单绩效奖金
   - 现状：将工资月份回拨到 `2026-03` 后，`t_salary_record` 已切到 3 月，但 `t_performance` 没有对应月份的绩效评分记录，导致后台薪资核算页的 3 月绩效奖金整列显示为 `0`。
   - 处理方向：直接通过 MySQL 为 `2026-03` 补齐一套完整绩效记录，并将工资表中的 `perf_bonus`、`gross_salary`、`net_salary` 同步更新，使 3 月工资单与绩效评分口径重新对齐。

84. [x] 工资月份回拨至 2026-03 并同步相关表
   - 现状：后台工资核算/发放相关演示数据仍挂在 `2026-04`，与“4 月期间应展示 3 月工资单、4 月工资需到 5 月才上传”的业务节奏不符。
   - 处理方向：直接通过 MySQL 将工资核算、发放记录、个税累计中的演示月份统一从 `2026-04` 调整为 `2026-03`，保持管理员端月份展示与答辩口径一致。

82. [x] 绩效等级字段扩容与历史绩效奖金数据回正
   - 现状：`t_performance.grade` 仍为 `varchar(2)`，保存“不合格”时会触发 Data too long；同时库中还残留切换到新绩效规则前的旧绩效奖金数据，表现为 `t_performance.perf_bonus_ratio` 与 `t_salary_record.perf_bonus` 口径不一致，部分工资单甚至在没有绩效评分记录时仍保留旧绩效奖金。
   - 处理方向：直接通过 MySQL 修正表结构与历史数据，将绩效等级字段扩容到可容纳完整中文等级，并把历史绩效记录与工资单中的绩效奖金统一校正到当前的 `900/1200/1500/0` 口径。

83. [x] 薪资重算保留非草稿流程状态
   - 现状：后端 `calculateSalary()` 在重算已有工资单时会统一把 `calcStatus` 重置为草稿，并清空工资条发布状态；这会影响历史月份、已审核或已发放工资单的流程完整性。
   - 处理方向：调整重算逻辑，仅新生成的工资单默认进入草稿；已有工资单重算时保留原 `calcStatus`、`slipPublished`、`slipPublishTime`、`payDate` 等流程字段，避免联动绩效或月底重算时把已走完流程的工资单打回初始状态。

81. [x] 后台图片预览层定位异常修复
   - 现状：管理员/经理页中从表格头像、公告封面、上传预览等位置点击图片放大时，预览层会被限制在表格或弹窗容器内部，出现图片压在表格中间、遮罩范围错误的异常展示。
   - 处理方向：统一修正 `el-image` 预览属性，改为使用 `preview-teleported` 将图片预览层挂载到 `body`，避免继续受局部容器的定位、滚动和裁剪影响。

80. [x] 经理薪资草稿区残留坏串清理
   - 现状：`static/admin/index.html` 经理薪资核算区域仍残留 `草60`、`?..` 等坏串，影响状态筛选、按钮名称、弹窗标题、错误提示与加载文案的可读性。
   - 处理方向：继续沿用 UTF-8 定点修复方式，将该区域残留坏串统一恢复为“草稿”相关中文文案，并补做脚本语法校验，避免再次破坏工资草稿流程。

79. [x] 登录页与管理员页中文乱码回退修复
   - 现状：`static/login.html` 与 `static/admin/index.html` 在近期多次页面文本调整后出现中文乱码或 `?` 占位，影响登录提示、首页公告标题、经理工资草稿按钮、个人中心字段、考勤规则、草稿提示语等关键界面文案。
   - 处理方向：不整页回退，保留已完成的登录自动分流、经理工资草稿、首页饼图百分比等功能，仅对损坏的中文文本做 UTF-8 定点修复，并补做脚本语法与 Maven 编译校验。

78. [x] 登录页角色选择移除与首页考勤饼图百分比化
   - 现状：登录页仍保留手动选择角色的下拉框，但系统实际上已按后端返回角色自动分流；管理员首页“当月整体考勤状态”饼图直接显示累计值，数字口径不直观。
   - 处理方向：删除登录页无效角色选择，仅保留账号/密码/验证码；首页考勤饼图改为百分比标签与提示，避免再将累计项数直接暴露给用户。

77. [x] 考勤异常率重整、早退字段落地与扣款规则公告化
   - 现状：2026-04 考勤记录异常占比偏低，页面缺少“早退”独立字段展示，迟到/早退/请假/旷工扣款口径也没有形成可演示的统一说明。
   - 处理方向：补齐 `t_attendance_record.early_leave_times` 与考勤规则字段，重整 2026-04 考勤与薪资关联数据，使异常率提升到约 5%；同步在管理员端、员工端、统计接口与单独公告中展示“早退”和扣款规则。

76. [x] 头像静态放行、性别匹配重排与公告列表发布时间对齐
   - 现状：`front_assets` 头像资源请求未被匿名放行，登录后员工/管理员表格中的头像与公告图片会直接“加载失败”；同时一批员工/经理头像存在性别错配和重复素材，公告管理列表页又没有显示发布时间，肉眼难以确认倒序是否生效。
   - 处理方向：在安全配置中补齐 `"/front_assets/**"` 放行；重排 `t_employee` / `t_user` 的头像路径，确保男/女员工分别绑定到不重复且性别匹配的头像；同时在管理员公告列表补上“发布时间”列，并继续保留前端按 `pubTime/createTime` 倒序兜底。

75. [x] 前端 CDN 依赖本地化，消除清缓存后首屏慢和断网不可用问题
   - 现状：`home.html`、`admin/index.html`、`login.html`、`index.html` 以及图标字体 CSS 直接引用公网 CDN / `at.alicdn.com`，浏览器一旦清缓存就要重新走外网拉取；断网时页面样式、脚本和图标会直接失效甚至白屏。
   - 处理方向：将 Vue、Element Plus、Axios、ECharts、XLSX 和 iconfont 字体资源统一落地到 `static/vendor`，前端入口页全部改走本地 `/api/vendor/**`，并同步补齐 Spring Security 对 `/vendor/**` 的匿名放行，确保离线环境也能正常打开页面。

74. [x] 头像资源恢复与公告发布时间倒序统一
   - 现状：员工/经理列表中的头像路径虽然仍在数据库中，但 `front_assets` 目录缺少大量对应头像文件，导致页面出现“加载失败”；同时公告列表在管理端与员工主页上没有稳定保证按最新发布时间倒序展示。
   - 处理方向：将缺失的员工/经理头像资源补回 `static/front_assets`，并对少量缺失编号头像改绑到现有未占用头像；同时统一后端公告查询和前端公告列表二次排序逻辑，确保所有公告页都按 `pub_time` 最新在最上。

73. [x] 演示主页内容补齐与员工业务数据批量充实
   - 现状：公告数量偏少且缺少封面图，员工/经理的考勤申请、薪资反馈、异常上报样本不足，难以支撑答辩演示；员工端首页“关于我们”也缺少稳定可用的详情入口，无法作为完整网站主页展示。
   - 处理方向：批量补充公告封面和公告内容，按员工维度补齐申请/反馈/异常数据，并把考勤记录扩展到更接近 5% 异常率的真实分布；同时将员工首页“关于我们详情”和网站配置服务端读取链路带回当前工作区，确保主页演示闭环可用。

> 用途：记录“视频 vs 当前程序”的差异清单，并在修复后更新 `[ ]` / `[x]`。

## 差异清单（待修复）

1. [x] **紧急修复**：全局修复 `year_month` (MySQL保留关键字) 导致的 500 SQLSyntaxErrorException
   - 现状：各类基于 `year_month` 过滤的列表/统计接口报错。
   - 实施子任务：
     - [x] Java实体层：所有含 `yearMonth` 属性的 Entity (`SalaryRecord`, `Performance`, `AttendanceRecord` 等) 强制加上 `@TableField("\`year_month\`")` 注解。
     - [x] XML映射层：所有手写 SQL (`*Mapper.xml`) 中的 `year_month` 全部替换为 `` `year_month` ``以转义。
     - [x] Wrapper构造器：排查 `QueryWrapper` 和代码中直接传入字面量 `"year_month"` 的场景，保证与带反引号的命名完全对齐。
2. [x] 薪资“二审/已审核(3)”流程缺失导致支付门控不可靠
   - 现状：`t_salary_record.calc_status` 语义包含 `3=已审核`，但业务只把发布置为 `2=待审核`，没有二审把状态置为 `3`；支付允许 `calcStatus>=2`。
   - 影响：可能绕过审核直接发放。
   - 实施子任务：
     - [x] 后端：新增 `/audit` 二审接口，补齐 `2 -> 3` 的状态转换。
     - [x] 后端：修改发钱接口 `paySalary`，拒绝 `calcStatus != 3` 的非合规请求，彻底堵死漏洞。
     - [x] 前端：新增高逼格弹窗版“模拟聚合支付收银台”（含微信/支付宝/银行网银假交互及成功动效）。
     - [x] 前端：补充列表顶部的【批量发放】按钮，打通并列验证门控。
2. [ ] 薪资反馈缺少流程联动（无法影响审核/阻塞发放）
   - 现状：`SalaryFeedbackServiceImpl.replyFeedback()` 仅更新反馈表状态与回复内容，不联动薪资账单状态与二审闭环。
   - 影响：反馈提交后系统仍可能直接进入发放流程，且“二审依据”缺失。
   - 相关代码：`SalaryFeedbackController`、`SalaryFeedbackServiceImpl`、二审接口（需要补齐）。
3. [ ] 薪资发放“发放文件 issueFile”字段链路缺失
   - 现状：前端“薪资发放”表格展示 `issueFile` 列，但 `SalaryPayment` 实体/`t_salary_payment` 表结构未见该字段。
   - 影响：视频里那种“发放文件/凭证”列无法落地展示。
   - 相关代码：DB（`t_salary_payment`）、`SalaryPayment` 实体、`SalaryPaymentServiceImpl`/发放逻辑、前端 `admin/index.html`。
4. [ ] 后端鉴权/状态校验不够一致（敏感操作可能被绕过）
   - 现状：前端按钮做了权限与状态隐藏，但后端在部分接口上缺少严格 role 校验与状态门控（避免“改请求直接操作”）。
   - 影响：安全性与业务一致性不足。
   - 相关代码：`SalaryController`、`SalaryFeedbackController`、`SalaryPaymentController` 以及其它涉及敏感操作的 controller/service。
5. [x] 全局 UI 统一化：操作按钮与表格分页组件规范化
   - 现状：部分表格缺失底层分页组件，操作按钮（详情、修改、删除等）颜色散乱无统一风格。
   - 影响：系统割裂感强，多组件切换显得不专业且容易引发多 Agent 修改造成的二次回归。
   - 实施：通过自动扫描注入 `p` 响应式状态实现纯前端切片分页，利用正则规范化 `primary/warning/danger/success` 的语义色值。
6. [ ] 员工侧完整流程页面（薪资详情/历史/反馈）可能存在占位未实现
   - 现状：目前仓库中的员工入口页面（如 `static/index.html`）大量模块是 Placeholder/TODO。
   - 影响：与视频展示的全流程不一致。
   - 相关代码：`salary-system/src/main/resources/static/index.html`（或其它员工前端资源）。

> 说明：详细“变更记录”已迁移到 `CHANGELOG.md`。

7. [x] 管理员端导出格式修复与算薪交互优化
   - 现状：薪资核算需强选月份交互不友好，各大表格只支持CSV导出。
   - 修复：增加当月缺省逻辑，全表导出切为 xlsx 格式。

8. [ ] 考勤自动化处理（打卡边界识别）
   - 现状：当前只是手工/直接填入迟到次数和加班小时数。
   - 目标：按照论文更改要求，支持通过导入包含打卡时间（09:00、18:00、19:00边界）的考勤源文件，自动算出本月迟到、早退（各扣50元）、加班（每满一小时加20元）的绝对值并注入基础数据。

9. [x] 绩效总分自动化评级与奖金直接挂钩 (四重累加分算法)
   - 现状：三项平均分算比例旧版逻辑，奖金按基数乘积比例发放，存在耦合且灵活性差。
   - 目标实现：废除“三项平均+奖罚”公式。采用“工作绩效(50) + 业务技能(10) + 工作态度(10) + 员工考勤(30)”的绝对值累加。
   - 评级调整：<=60(不合格/无), 61-75(合格), 76-85(良好), 86-100(优秀)。金额前端控制，映射后以绝对值落库至 `perfBonusRatio` 字段并在薪资核算时直接提取。

10. [x] 五险一金依据“基本工资”正向计提及失业保险展示
   - 现状：旧版由于后端直接通过 `17.5%` 返回扣划总额，导致前端明细受限及计算逻辑绕弯。
   - 目标实现：在前端直接依靠 `row.baseSalary` 提取：“养老(8%)、公积金(7%)、医疗(2%)、失业(0.5%新增展示列)”，不仅补充了失业保险列，还修复了经理账号和姓名的缺省显示。

11. [ ] 部门经理“一键上报/汇总”闭环交互
   - 现状：经理虽然有批量计算的能力，但在UI上没有一键整体发布的显性闭环链路或总览报表。
   - 目标：允许经理通过“一键确认发布”将部门下所有人薪水直接推送上报。管理员拥有最终支付权。
