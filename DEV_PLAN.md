# DEV_PLAN
111. [x] 薪资审核补齐“驳回退回经理修改”链路
   - 现状：图 3-7 中已包含“管理员审核不通过 -> 退回经理修改”的业务分支，但系统真实实现只有“待审核 -> 已审核 -> 已发放”，缺少驳回接口、前端按钮和重提审核入口，导致程序与论文流程图不一致。
   - 处理方向：新增管理员单笔/批量驳回接口，将 `2=待审核` 退回为 `5=已驳回`；前端补“驳回/批量驳回”按钮、状态标签与筛选项；同时允许经理对“已驳回”记录继续编辑并重新提交审核。
110. [x] 薪资核算流程图右侧“继续编辑草稿”回路改为完整正交连通路径
   - 现状：用户开始单独检查 `07` 流程图，并继续要求右侧“否 -> 继续编辑草稿 -> 经理编辑个别草稿”回路画成真正连通的横平竖直箭头，不要看起来像断开的折线。
   - 处理方向：继续微调 `07-图3-7-薪资核算与审核发放流程图.png`，将右侧否分支改为“水平到框下方、竖直到继续编辑草稿、再水平返回经理编辑个别草稿”的完整正交路径。

109. [x] 登录流程图失败回路继续收敛为单拐点路径
   - 现状：用户继续指出 `06` 登录流程图中左右失败回路仍存在多余折返，希望在提示框下方只保留一个拐点后再进入提示框。
   - 处理方向：继续压缩 `06-图3-6-登录与角色分流流程图.png` 的左右失败分支，改成“判断框横向到提示框下方，再单次上拐进入提示框”的单拐点路径。

108. [x] 登录流程图失败分支改为“经提示框再返回输入框”的两段式路径
   - 现状：用户继续要求 `06` 登录流程图中的失败分支不要直接贴着提示框侧边进入，而应先走到提示框下方，再进入提示框，最后再从提示框水平返回输入框。
   - 处理方向：继续微调 `06-图3-6-登录与角色分流流程图.png` 的左右失败回路，使其分为“判断框 -> 提示框”和“提示框 -> 输入框”两段路径，贴合用户手绘示意。

107. [x] 登录流程图提示框与输入框改为同层水平回流
   - 现状：用户继续指出 `06` 登录流程图中左右提示框虽然已回接输入框，但提示框本体仍未与输入框处于同一水平层，视觉上仍不够“直着过来”。
   - 处理方向：将左右提示框整体上移到与输入框同一水平线，并保留从提示框直接水平箭头返回输入框的表达。

106. [x] 论文流程图 6/7 二次重排：修复顶部箭头重叠与右侧回流折线
   - 现状：用户继续指出 `06` 登录流程图顶部进入输入框处存在箭头重叠不清的问题，同时 `07` 薪资核算流程图右侧“继续编辑草稿”回流到“经理编辑个别草稿”的线不应折弯绕回。
   - 处理方向：重排两张流程图布局，`06` 将失败回流改接到输入框左右侧，避开顶部主箭头；`07` 将“继续编辑草稿”提升到与“经理编辑个别草稿”同一横向层级，并同步修正左下“退回经理修改”分支的回流表达。

105. [x] 论文流程图 6/7 改为横平竖直箭头版
   - 现状：用户要求 `06-图3-6-登录与角色分流流程图.png` 与 `07-图3-7-薪资核算与审核发放流程图.png` 按正式论文风格继续优化，连线必须横平竖直且带箭头。
   - 处理方向：继续微调绘图脚本，将两张流程图中失败回路、分支回流与主流程链统一改为正交流程箭头，避免继续出现无箭头折线或斜向回路。

103. [x] 论文 ER 图基数纠正：员工对考勤/绩效为一对多，工资单对反馈按论文口径收为一对一
   - 现状：用户指出数据库总体 ER 图中的两个基数关系有误：一个员工应对应多条考勤记录；论文口径下单条工资记录只对应一条薪资反馈。
   - 处理方向：修正 `08-图3-8-数据库总体-ER-图.png` 中 `员工-考勤记录`、`员工-绩效记录`、`薪资记录-薪资反馈` 三处基数标注，使图面表达与当前论文业务口径一致。

104. [ ] 后端“工资单一单一反馈”约束待与论文口径对齐
   - 现状：论文 ER 图现已按“一条工资记录对应一条薪资反馈”展示，但当前数据库表 `t_salary_feedback` 仅通过 `salary_record_id` 普通外键关联工资单，未看到唯一约束，系统层面仍可能产生一条工资单对应多条反馈。
   - 处理方向：后续如需让系统实现与论文图完全一致，需要补查后端提交反馈逻辑，并考虑为 `salary_record_id` 增加唯一约束或服务层幂等校验。

102. [x] 论文 ER 图继续上移员工实体并加大与薪资记录的竖向间距
   - 现状：用户继续指出 `员工` 方框仍不够高，且与 `薪资记录` 的垂直距离仍偏近，希望进一步上移员工实体并拉开上下两块的留白。
   - 处理方向：继续微调 `08-图3-8-数据库总体-ER-图.png` 的中心布局，上移 `员工` 方框本体与其上下属性区，下移 `生成` 到 `薪资记录` 的竖向链路，增加两者之间的视觉间距。

101. [x] 论文参考文献扩充至24篇并上移 ER 图中的员工实体
   - 现状：用户明确要求将参考文献总数从 20 篇改为 24 篇，同时保持外文最多 4 篇；并要求数据库总体 ER 图中的 `员工` 实体整体上移，使中心布局更紧凑。
   - 处理方向：在保留原 4 篇外文的前提下，为 `文献池清单.md` 与 `论文正文草稿.md` 新增 4 篇中文参考文献；同步上移 `08-图3-8-数据库总体-ER-图.png` 中 `员工` 实体及其上下关联元素的位置。

100. [x] 论文 ER 图中心实体与关系菱形改为单段直线连接
   - 现状：用户进一步要求数据库总体 ER 图中 `员工` 实体与上方/左右关系菱形之间不要再出现折线，两个部件之间必须使用单段直线连接。
   - 处理方向：继续微调 `08-图3-8-数据库总体-ER-图.png` 的中心布局，保留 `员工` 居中和属性上下分流，但将 `绑定`、`归属`、`形成`、`参与` 到 `员工` 的连线统一改成单段直线。

99. [x] 论文 ER 图属性分流重排与参考文献外文比例收紧
   - 现状：用户继续指出数据库总体 ER 图中“员工”“薪资记录”两个实体的属性区不应全部堆在同一侧，要求改成上下分流以避免连接线压住属性；同时明确要求参考文献不要保留过多外文，国外文献最多 4 篇，总量维持 20 篇左右即可。
   - 处理方向：将 `08-图3-8-数据库总体-ER-图.png` 中 `员工` 改为上 3 下 2、`薪资记录` 改为上下分布，并重新整理 `文献池清单.md` 与 `论文正文草稿.md` 的参考文献结构，压缩外文为 4 篇、中文为主。

98. [x] 论文图表三次微调：权限结构图改 H 图，ER 图补足核心属性
   - 现状：用户确认图 3.1 的 H 图风格可保留，并继续要求图 3.2 也改成 H 图表达；同时指出数据库总体 ER 图属性过少，希望更贴近系统真实表结构。
   - 处理方向：将 `02-图3-2-权限与角色结构图.png` 调整为与图 3.1 同风格的 H 图；将 `08-图3-8-数据库总体-ER-图.png` 扩充为核心属性增强版，按真实数据库字段补充账号、员工、考勤、绩效、薪资、公告、反馈等实体的关键属性，但本轮仍只更新 PNG，不回嵌 Word。

97. [x] 论文图确认前暂停回嵌主文档，仅迭代 PNG 成图
   - 现状：用户已确认 H 图可以保留，但要求后续图片在其确认“可以了”之前，先只改 PNG，不要继续放入论文主文档。
   - 处理方向：后续图表迭代阶段仅更新 `codexlunwen/assets/mermaid/*.png` 与绘图脚本，不再自动重建主论文 `docx`；待用户确认整套图片通过后，再统一嵌回 Word。

96. [x] 论文图表二次修正：功能模块图竖排、用例图去框、登录流程序化、ER 图原创化
   - 现状：上一版虽然已按论文图范式重做，但用户继续指出 4 个具体问题：功能模块图子框仍不是样例中的竖排窄框样式；用例图不需要系统边界框；登录流程图多了“管理员登录”框；ER 图仍容易被误认为沿用了用户样图结构。
   - 处理方向：继续定点修改绘图脚本，将功能模块图改为一级/二级模块均使用竖排窄框，用例图去掉演员名方框与系统边界框，登录流程图从“开始”直接进入输入框，并按系统真实业务重新组织原创 ER 图后重新嵌回论文。

95. [x] 论文第3章图表按样例重做为标准论文图范式
   - 现状：上一轮虽然已将第 3 章图替换进正文，但整体仍偏 Mermaid 工程示意图，不符合用户给出的样例范式；尤其功能模块图、用例图、流程图和 ER 图的图形元素与版式都不够像正式论文配图。
   - 处理方向：放弃继续微调 Mermaid 出图，直接改为手工绘制黑白论文图，统一采用树状矩形功能模块图、标准 UML 用例图、标准流程图和 Chen 风格 ER 图，并重新覆盖 PNG 后回嵌主论文 docx。

94. [x] 论文第3章 UML 图、功能模块图与 ER 图重绘并回嵌正文
   - 现状：原论文第 3 章虽然已有 Mermaid 图，但角色用例图不够像标准 UML，用例演员标签也不够规范，数据库 ER 图信息量偏少，功能模块图的层次表达也不够正式。
   - 处理方向：按系统真实功能重绘图 3.1、图 3.3、图 3.4、图 3.5、图 3.8，统一为黑白论文风格，并重新生成主论文 `docx` 与图源码附件，确保图源码、PNG 和正文 Word 同步更新。

93. [x] 图表绘制 MCP 安装与多 Agent 共享接入
   - 现状：当前 Codex 只内置了 Playwright、MySQL 等 MCP，缺少适合绘制 UML、功能模块图、流程图、ER 图的专用图形工具。
   - 处理方向：安装并接入 `plantuml-mcp-server` 与 `mcp-mermaid`，写入用户级 `C:\Users\614377781\.codex\config.toml`，并在工作区补充 `diagram-mcp` 技能说明，方便后续其他 agent 直接复用。

92. [x] 论文终稿、图源码附件与参考文献清单交付
   - 现状：`codexlunwen` 目录下已完成论文中间产物，但还未真正输出可提交的主论文 `.docx`、图源码附件 `.docx` 和参考文献规范化 `.xlsx` 成品。
   - 处理方向：基于已冻结的系统事实、样式分析、图表清单和文献池，生成正式论文成品文件，补齐图源码附件与参考文献清单，并将交付结果回写状态文件，避免论文任务只停留在 Markdown 草稿阶段。
   - 补充说明：已继续修复样式抽取误带来的正文段前分页问题，重新输出“分页修复带图版”论文，避免 Word 中出现大段空白和图文被强制拆页。
   - 二次修订：已按论文口吻修正中文摘要措辞，去掉摘要中的“了”字；同时将第 3 章图表重新调整为 HIPO 功能图、角色用例图和标准流程图风格，并重新渲染输出新的论文修正版。
   - 三次修订：已继续将第 3 章系统图统一优化为黑白极简风格，并通过 Mermaid `linear + elk` 布局尽量收敛为直线、正交连线，新增输出 `thesis-figure-bw-straight.docx` 作为当前最新图表修正版。

91. [x] 经理个人中心资料空白与员工端按钮类型警告修复
   - 现状：部门经理账号进入共享后台后，个人中心中的姓名、性别、手机、身份证号、部门、银行卡号、岗位、入职时间等字段全部显示为 `-`；同时员工端修改密码按钮写成了 `type=" primary"`，Element Plus 控制台持续报按钮类型校验 warning。
   - 处理方向：修复 `admin/index.html` 中 `loadSelfInfo()` 已损坏的取数逻辑，统一用 `/api/auth/me` 与当前登录态回填 `selfInfo/selfEditForm`，保证经理个人中心可正常显示和编辑本人档案；同步修正 `home.html` 按钮 `type` 的多余空格，消除员工端控制台警告。

90. [x] 管理员端考勤/薪资核算/薪资发放表格未铺满修复
   - 现状：管理员端“考勤数据”“薪资核算”“薪资发放”三个列表页因为列几乎全部使用固定 `width`，导致表格右侧出现大块留白，未能铺满卡片容器。
   - 处理方向：保留原有列顺序与交互，只将这三张表中的部分固定列宽调整为 `min-width`，给 Element Plus 留出可伸缩列，让表格随容器宽度自动拉伸铺满。


89. [x] 扩展外部技能库：集成论文撰写与知网搜索插件
   - 实施内容：从 GitHub 克隆了 lunwen-skill、cnki-skills、gs-skills 三个专业组件到 e:\bishe\.agents\skills，为后续多 Agent 协同撰写毕业论文和检索学术资料提供了底层 SOP 支持。

87. [x] 2026-03 薪资/绩效/考勤/发放重复脏数据清理与引用回绑
   - 现状：`2026-03` 同月存在两套员工工资单、绩效记录、考勤记录；发放表金额与工资表 `net_salary` 大面积不一致，且薪资反馈引用了即将清理的旧工资单 ID。
   - 处理方向：保留后一套自洽的 3 月数据，删除旧重复工资/绩效/考勤记录；同步修正经理工资单、发放记录金额与反馈表 `salary_record_id` 引用，确保 3 月只保留 15 条一致数据。

88. [x] 2026-01/2026-02 经理工资与个税累计链路待补齐
   - 现状：`2026-01` 已补齐 15 条工资单，但 `2026-02` 仍缺经理账号 `10001`-`10005` 的工资记录；同时 `t_tax_accumulate` 在 2026 年 1-3 月仅残留少量旧记录，和当前工资表链路不一致。
   - 处理方向：直接复用已存在的 2026-02 经理绩效/考勤数据补齐缺失工资单，并按当前工资表重建 2026 年 1-3 月的累计个税记录，同步补齐 1、2 月发放记录，确保工资、支付、个税三张表口径一致。

86. [x] 管理员页薪资反馈列表头像兜底修复
   - 现状：管理员端“薪资反馈”列表最后几条记录未显示真实头像，只回退到首字母头像；问题集中在工号 `10001`、`10003`、`10004` 等记录。
   - 处理方向：统一薪资反馈列表的员工资料解析链，改为复用 `resolveEmployeeProfile(...)` 与 `getStoredAvatar(...)`，让员工、经理账号都能按工号/账号正确回填头像、姓名与工号。

85. [x] 2026-03 绩效记录补齐并回写工资单绩效奖金
   - 现状：将工资月份回拨到 `2026-03` 后，`t_salary_record` 已切到 3 月，但 `t_performance` 没有对应月份的绩效评分记录，导致后台薪资核算页的 3 月绩效奖金整列显示为 `0`。
   - 处理方向：直接通过 MySQL 为 `2026-03` 补齐一套完整绩效记录，并将工资表中的 `perf_bonus`、`gross_salary`、`net_salary` 同步更新，使 3 月工资单与绩效评分口径重新对齐。

84. [x] 工资月份回拨至 2026-03 并同步相关表
   - 现状：后台工资核算/发放相关演示数据仍挂在 `2026-04`，与“4 月期间应展示 3 月工资单、4 月工资需到 5 月才上传”的业务节奏不符。
   - 处理方向：直接通过 MySQL 将工资核算、发放记录、个税累计中的演示月份统一从 `2026-04` 调整为 `2026-03`，保持管理员端月份展示与答辩口径一致。

82. [x] 绩效等级字段扩容与历史绩效奖金数据回正
   - 现状：`t_performance.grade` 仍为 `varchar(2)`，保存“不合格”时会触发 Data too long；同时库中还残留切换到新绩效规则前的旧绩效奖金数据，表现为 `t_performance.perf_bonus_ratio` 与 `t_salary_record.perf_bonus` 口径不一致，部分工资单甚至在没有绩效评分记录时仍保留旧绩效奖金。
   - 处理方向：直接通过 MySQL 修正表结构与历史数据，将绩效等级字段扩容到可容纳完整中文等级，并把历史绩效记录与工资单中的绩效奖金统一校正到当前的 `900/1200/1500/0` 口径。

83. [x] 薪资重算保留非草稿流程状态
   - 现状：后端 `calculateSalary()` 在重算已有工资单时会统一把 `calcStatus` 重置为草稿，并清空工资条发布状态；这会影响历史月份、已审核或已发放工资单的流程完整性。
   - 处理方向：调整重算逻辑，仅新生成的工资单默认进入草稿；已有工资单重算时保留原 `calcStatus`、`slipPublished`、`slipPublishTime`、`payDate` 等流程字段，避免联动绩效或月底重算时把已走完流程的工资单打回初始状态。

81. [x] 后台图片预览层定位异常修复
   - 现状：管理员/经理页中从表格头像、公告封面、上传预览等位置点击图片放大时，预览层会被限制在表格或弹窗容器内部，出现图片压在表格中间、遮罩范围错误的异常展示。
   - 处理方向：统一修正 `el-image` 预览属性，改为使用 `preview-teleported` 将图片预览层挂载到 `body`，避免继续受局部容器的定位、滚动和裁剪影响。

80. [x] 经理薪资草稿区残留坏串清理
   - 现状：`static/admin/index.html` 经理薪资核算区域仍残留 `草60`、`?..` 等坏串，影响状态筛选、按钮名称、弹窗标题、错误提示与加载文案的可读性。
   - 处理方向：继续沿用 UTF-8 定点修复方式，将该区域残留坏串统一恢复为“草稿”相关中文文案，并补做脚本语法校验，避免再次破坏工资草稿流程。

79. [x] 登录页与管理员页中文乱码回退修复
   - 现状：`static/login.html` 与 `static/admin/index.html` 在近期多次页面文本调整后出现中文乱码或 `?` 占位，影响登录提示、首页公告标题、经理工资草稿按钮、个人中心字段、考勤规则、草稿提示语等关键界面文案。
   - 处理方向：不整页回退，保留已完成的登录自动分流、经理工资草稿、首页饼图百分比等功能，仅对损坏的中文文本做 UTF-8 定点修复，并补做脚本语法与 Maven 编译校验。

78. [x] 登录页角色选择移除与首页考勤饼图百分比化
   - 现状：登录页仍保留手动选择角色的下拉框，但系统实际上已按后端返回角色自动分流；管理员首页“当月整体考勤状态”饼图直接显示累计值，数字口径不直观。
   - 处理方向：删除登录页无效角色选择，仅保留账号/密码/验证码；首页考勤饼图改为百分比标签与提示，避免再将累计项数直接暴露给用户。

77. [x] 考勤异常率重整、早退字段落地与扣款规则公告化
   - 现状：2026-04 考勤记录异常占比偏低，页面缺少“早退”独立字段展示，迟到/早退/请假/旷工扣款口径也没有形成可演示的统一说明。
   - 处理方向：补齐 `t_attendance_record.early_leave_times` 与考勤规则字段，重整 2026-04 考勤与薪资关联数据，使异常率提升到约 5%；同步在管理员端、员工端、统计接口与单独公告中展示“早退”和扣款规则。

76. [x] 头像静态放行、性别匹配重排与公告列表发布时间对齐
   - 现状：`front_assets` 头像资源请求未被匿名放行，登录后员工/管理员表格中的头像与公告图片会直接“加载失败”；同时一批员工/经理头像存在性别错配和重复素材，公告管理列表页又没有显示发布时间，肉眼难以确认倒序是否生效。
   - 处理方向：在安全配置中补齐 `"/front_assets/**"` 放行；重排 `t_employee` / `t_user` 的头像路径，确保男/女员工分别绑定到不重复且性别匹配的头像；同时在管理员公告列表补上“发布时间”列，并继续保留前端按 `pubTime/createTime` 倒序兜底。

75. [x] 前端 CDN 依赖本地化，消除清缓存后首屏慢和断网不可用问题
   - 现状：`home.html`、`admin/index.html`、`login.html`、`index.html` 以及图标字体 CSS 直接引用公网 CDN / `at.alicdn.com`，浏览器一旦清缓存就要重新走外网拉取；断网时页面样式、脚本和图标会直接失效甚至白屏。
   - 处理方向：将 Vue、Element Plus、Axios、ECharts、XLSX 和 iconfont 字体资源统一落地到 `static/vendor`，前端入口页全部改走本地 `/api/vendor/**`，并同步补齐 Spring Security 对 `/vendor/**` 的匿名放行，确保离线环境也能正常打开页面。

74. [x] 头像资源恢复与公告发布时间倒序统一
   - 现状：员工/经理列表中的头像路径虽然仍在数据库中，但 `front_assets` 目录缺少大量对应头像文件，导致页面出现“加载失败”；同时公告列表在管理端与员工主页上没有稳定保证按最新发布时间倒序展示。
   - 处理方向：将缺失的员工/经理头像资源补回 `static/front_assets`，并对少量缺失编号头像改绑到现有未占用头像；同时统一后端公告查询和前端公告列表二次排序逻辑，确保所有公告页都按 `pub_time` 最新在最上。

73. [x] 演示主页内容补齐与员工业务数据批量充实
   - 现状：公告数量偏少且缺少封面图，员工/经理的考勤申请、薪资反馈、异常上报样本不足，难以支撑答辩演示；员工端首页“关于我们”也缺少稳定可用的详情入口，无法作为完整网站主页展示。
   - 处理方向：批量补充公告封面和公告内容，按员工维度补齐申请/反馈/异常数据，并把考勤记录扩展到更接近 5% 异常率的真实分布；同时将员工首页“关于我们详情”和网站配置服务端读取链路带回当前工作区，确保主页演示闭环可用。

> 用途：记录“视频 vs 当前程序”的差异清单，并在修复后更新 `[ ]` / `[x]`。

## 差异清单（待修复）

1. [x] **紧急修复**：全局修复 `year_month` (MySQL保留关键字) 导致的 500 SQLSyntaxErrorException
   - 现状：各类基于 `year_month` 过滤的列表/统计接口报错。
   - 实施子任务：
     - [x] Java实体层：所有含 `yearMonth` 属性的 Entity (`SalaryRecord`, `Performance`, `AttendanceRecord` 等) 强制加上 `@TableField("\`year_month\`")` 注解。
     - [x] XML映射层：所有手写 SQL (`*Mapper.xml`) 中的 `year_month` 全部替换为 `` `year_month` ``以转义。
     - [x] Wrapper构造器：排查 `QueryWrapper` 和代码中直接传入字面量 `"year_month"` 的场景，保证与带反引号的命名完全对齐。
2. [x] 薪资“二审/已审核(3)”流程缺失导致支付门控不可靠
   - 现状：`t_salary_record.calc_status` 语义包含 `3=已审核`，但业务只把发布置为 `2=待审核`，没有二审把状态置为 `3`；支付允许 `calcStatus>=2`。
   - 影响：可能绕过审核直接发放。
   - 实施子任务：
     - [x] 后端：新增 `/audit` 二审接口，补齐 `2 -> 3` 的状态转换。
     - [x] 后端：修改发钱接口 `paySalary`，拒绝 `calcStatus != 3` 的非合规请求，彻底堵死漏洞。
     - [x] 前端：新增高逼格弹窗版“模拟聚合支付收银台”（含微信/支付宝/银行网银假交互及成功动效）。
     - [x] 前端：补充列表顶部的【批量发放】按钮，打通并列验证门控。
2. [ ] 薪资反馈缺少流程联动（无法影响审核/阻塞发放）
   - 现状：`SalaryFeedbackServiceImpl.replyFeedback()` 仅更新反馈表状态与回复内容，不联动薪资账单状态与二审闭环。
   - 影响：反馈提交后系统仍可能直接进入发放流程，且“二审依据”缺失。
   - 相关代码：`SalaryFeedbackController`、`SalaryFeedbackServiceImpl`、二审接口（需要补齐）。
3. [ ] 薪资发放“发放文件 issueFile”字段链路缺失
   - 现状：前端“薪资发放”表格展示 `issueFile` 列，但 `SalaryPayment` 实体/`t_salary_payment` 表结构未见该字段。
   - 影响：视频里那种“发放文件/凭证”列无法落地展示。
   - 相关代码：DB（`t_salary_payment`）、`SalaryPayment` 实体、`SalaryPaymentServiceImpl`/发放逻辑、前端 `admin/index.html`。
4. [ ] 后端鉴权/状态校验不够一致（敏感操作可能被绕过）
   - 现状：前端按钮做了权限与状态隐藏，但后端在部分接口上缺少严格 role 校验与状态门控（避免“改请求直接操作”）。
   - 影响：安全性与业务一致性不足。
   - 相关代码：`SalaryController`、`SalaryFeedbackController`、`SalaryPaymentController` 以及其它涉及敏感操作的 controller/service。
5. [x] 全局 UI 统一化：操作按钮与表格分页组件规范化
   - 现状：部分表格缺失底层分页组件，操作按钮（详情、修改、删除等）颜色散乱无统一风格。
   - 影响：系统割裂感强，多组件切换显得不专业且容易引发多 Agent 修改造成的二次回归。
   - 实施：通过自动扫描注入 `p` 响应式状态实现纯前端切片分页，利用正则规范化 `primary/warning/danger/success` 的语义色值。
6. [ ] 员工侧完整流程页面（薪资详情/历史/反馈）可能存在占位未实现
   - 现状：目前仓库中的员工入口页面（如 `static/index.html`）大量模块是 Placeholder/TODO。
   - 影响：与视频展示的全流程不一致。
   - 相关代码：`salary-system/src/main/resources/static/index.html`（或其它员工前端资源）。

> 说明：详细“变更记录”已迁移到 `CHANGELOG.md`。

7. [x] 管理员端导出格式修复与算薪交互优化
   - 现状：薪资核算需强选月份交互不友好，各大表格只支持CSV导出。
   - 修复：增加当月缺省逻辑，全表导出切为 xlsx 格式。

8. [ ] 考勤自动化处理（打卡边界识别）
   - 现状：当前只是手工/直接填入迟到次数和加班小时数。
   - 目标：按照论文更改要求，支持通过导入包含打卡时间（09:00、18:00、19:00边界）的考勤源文件，自动算出本月迟到、早退（各扣50元）、加班（每满一小时加20元）的绝对值并注入基础数据。

9. [x] 绩效总分自动化评级与奖金直接挂钩 (四重累加分算法)
   - 现状：三项平均分算比例旧版逻辑，奖金按基数乘积比例发放，存在耦合且灵活性差。
   - 目标实现：废除“三项平均+奖罚”公式。采用“工作绩效(50) + 业务技能(10) + 工作态度(10) + 员工考勤(30)”的绝对值累加。
   - 评级调整：<=60(不合格/无), 61-75(合格), 76-85(良好), 86-100(优秀)。金额前端控制，映射后以绝对值落库至 `perfBonusRatio` 字段并在薪资核算时直接提取。

10. [x] 五险一金依据“基本工资”正向计提及失业保险展示
   - 现状：旧版由于后端直接通过 `17.5%` 返回扣划总额，导致前端明细受限及计算逻辑绕弯。
   - 目标实现：在前端直接依靠 `row.baseSalary` 提取：“养老(8%)、公积金(7%)、医疗(2%)、失业(0.5%新增展示列)”，不仅补充了失业保险列，还修复了经理账号和姓名的缺省显示。

11. [ ] 部门经理“一键上报/汇总”闭环交互
   - 现状：经理虽然有批量计算的能力，但在UI上没有一键整体发布的显性闭环链路或总览报表。
   - 目标：允许经理通过“一键确认发布”将部门下所有人薪水直接推送上报。管理员拥有最终支付权。
